# 🐼 Pandas — Data Analysis & Manipulation

This folder contains my learning, practice, and notes on **Pandas**, one of the most important Python libraries for data analysis and machine learning.

The goal of this section was to learn the practical Pandas fundamentals required for working with real-world datasets and preparing data for Machine Learning.

---

## 📚 Topics Covered

### 1. Introduction to Pandas
- What is Pandas?
- Applications of Pandas
- Importing Pandas
- Understanding Pandas data structures

### 2. Pandas Series
- Creating Series
- Understanding 1D data
- Series indexing
- Working with Series values

### 3. Pandas DataFrame
- Creating DataFrames
- Understanding rows and columns
- Creating DataFrames from dictionaries
- Basic DataFrame operations

### 4. Loading Data
- Reading CSV files
- Reading Excel files
- Working with external datasets
- Writing data to files

### 5. Exploring Data
- `head()`
- `tail()`
- `info()`
- `describe()`
- `shape`
- `columns`
- `index`
- `dtypes`

### 6. Data Selection
- Selecting individual columns
- Selecting multiple columns
- Selecting rows
- `loc[]`
- `iloc[]`
- Index-based selection

### 7. Handling Missing Data
- Identifying missing values
- `isnull()`
- `notnull()`
- `dropna()`
- `fillna()`
- Replacing missing values
- Understanding appropriate missing-value strategies

### 8. Data Cleaning
- Renaming columns
- Removing duplicate records
- Replacing values
- Changing data types
- Basic dataset cleaning

### 9. Data Manipulation
- Filtering data
- Sorting data
- Selecting specific records
- Applying transformations
- Working with columns and indexes

### 10. Merging & Concatenation
- Combining DataFrames
- `merge()`
- `concat()`
- Understanding how datasets can be combined

### 11. Grouping & Aggregation
- `groupby()`
- Aggregation functions
- `sum()`
- `mean()`
- `count()`
- `min()`
- `max()`

### 12. Useful Data Operations
- `value_counts()`
- `sort_values()`
- `drop_duplicates()`
- `astype()`
- `apply()`
- `map()`
- `replace()`

---

## 🧪 Practice Dataset

During the learning process, I worked with sample datasets including:

- California Housing Dataset
- User / Customer dataset
- CSV files
- Excel files

These datasets were used to practice:

- Data inspection
- Data selection
- Data cleaning
- Missing-value handling
- Data manipulation
- Combining datasets

---

## 🛠️ Tools & Technologies

- Python
- Pandas
- Jupyter Notebook
- Google Colab
- Git
- GitHub

---

## 📂 Folder Structure

```text
03_PANDAS/
│
├── PandasTutorial.ipynb
└── README.md
